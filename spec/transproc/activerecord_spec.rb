require "spec_helper"

RSpec.describe Transproc::Activerecord do
  it "has a version number" do
    expect(Transproc::Activerecord::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
